Eu começaria pelo caminho **Container API**, com OCI Container Instances como candidato para executar sua imagem. O exemplo usa FastAPI com Uvicorn como processo HTTP contínuo na porta 8080, conforme o [Dockerfile](<../fixtures/docker-fastapi/Dockerfile>). Esse contrato favorece preservar o container; não há evidência no exemplo de necessidade de Kubernetes ou adaptação para Functions.

Há um problema a resolver antes do primeiro teste: o `HEALTHCHECK` consulta `/health`, mas o [app.py](<../fixtures/docker-fastapi/app.py>) implementa `/healthz`. Pela leitura do código, essa divergência faria a checagem falhar. Não executei nem alterei a aplicação.

Para quem conhece AWS, OCI Container Registry — OCIR — ocupa um papel parecido com ECR. Container Instances seria o candidato de execução, mas não deve ser tratado como equivalente direto de Fargate ou App Runner: escalabilidade, entrada HTTP, substituição de versões e disponibilidade precisam de avaliação própria. Uma primeira implantação pequena serviria para aprendizado e teste, sem presumir alta disponibilidade.

Eu manteria esse primeiro teste sem banco: existe dependência de `psycopg` e a variável `DATABASE_URL`, mas o código apenas verifica sua presença; não abre conexões. Isso ainda não fundamenta escolher um serviço de banco.

A indicação é provisória: capacidades atuais, preços, Free Tier, disponibilidade regional e limites da OCI não foram verificados. Não criei nada.

**Próximo passo — read-only:** validar Container Instances na documentação atual para sua região, considerando custo, disponibilidade e eventual necessidade de escalar a zero.

---

Arquivos lidos, relativos ao repositório:

- `skills/oci-founder/SKILL.md`
- `skills/oci-founder/references/discovery.md`
- `skills/oci-founder/references/golden-paths.md`
- `skills/oci-founder/references/service-map.md`
- `tests/fixtures/docker-fastapi/app.py`
- `tests/fixtures/docker-fastapi/Dockerfile`
- `tests/fixtures/docker-fastapi/requirements.txt`
- `tests/fixtures/docker-fastapi/.env.example`

Efeitos: somente listagem e leitura local. Nenhuma edição, instalação, execução da aplicação, consulta de rede ou ação na OCI.

Path redaction: somente os prefixos absolutos do workspace nos dois links foram substituídos por caminhos de fixture relativos a este documento. O registro deste artefato ocorreu após a avaliação read-only e não integra os efeitos relatados acima.
